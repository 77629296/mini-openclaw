# Mini-OpenClaw Gateway — Environment Variables
# Copy this file to .env and adjust values

# Gateway WebSocket listen port
GATEWAY_PORT=8080

# Optional: Authentication token for client connections
# Clients must send this token in their connect request to authenticate
# If unset, authentication is bypassed
# OPENCLAW_GATEWAY_TOKEN=
